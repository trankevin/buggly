import PageTitle from "components/PageTitle/PageTitle";

export default function DashboardPage({bugList, handleAddFormSubmit, handleDelete, handleUpdate}) {

    return (
        <>
        <PageTitle title="Dashboard"/>
        
        </>
    );
}